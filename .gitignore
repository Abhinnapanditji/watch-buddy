# -------------------------------------------------------------
# 1. DEPENDENCIES / PACKAGES
# -------------------------------------------------------------

# Dependencies (Node)
# Ignore all node_modules directories anywhere in the project
# This is usually the largest folder and should never be committed.
**/node_modules
# Yarn / pnpm / npm cache and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm
.yarn

# Dependency cache/installation artifacts (Common to all systems)
# pnpm store files
.pnpm-store
# Composer (PHP/Backend dependencies if applicable)
/vendor

# -------------------------------------------------------------
# 2. BUILD ARTIFACTS / COMPILER OUTPUTS
# -------------------------------------------------------------

# Distribution (compiled/transpiled code)
/dist
/build
/out
/public/build
/coverage
*.tsbuildinfo

# Common language-specific compiled files
# Python
__pycache__/
*.pyc

# -------------------------------------------------------------
# 3. ENVIRONMENT & SECRETS
# -------------------------------------------------------------

# Local environment variables and secrets (NEVER COMMIT THESE!)
# The content of these files is machine/deployment specific.
.env
.env.local
.env.*.local
.DS_Store

# Include an example for others to copy
!.env.example
!.env.development.example

# Visual Studio Code
# Ignore the entire workspace directory, but keep the shared
# extension list for team consistency.
.vscode/
!.vscode/extensions.json
# VS Code local workspace history
.history/

# JetBrains IDEs (IntelliJ, WebStorm, PyCharm, etc.)
.idea/
